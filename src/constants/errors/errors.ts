export const BAD_REQUEST = (status: number) => `Bad request, status: ${status}. Try again later.`;
export const NOT_ATHORIZED = 'User not authorized';

export const ERROR_COMPONENT_TEMPLATE = (statusCode: number) =>
  `Oops some error occured. Status code: ${statusCode}`;
